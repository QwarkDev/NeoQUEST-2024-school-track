# Используем официальный образ PHP
FROM php:7.4-cli

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем исходный код проекта в контейнер
COPY . /app

# Устанавливаем команду для запуска сервера PHP
CMD ["php", "-S", "0.0.0.0:1637"]